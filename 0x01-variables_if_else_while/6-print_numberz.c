#include <stdio.h>

/**
 * main - this is the main class
 * it takes no arguments
 *
 *printing numbers from their ASCII values
 *
 *Return: 0
 **/

int main(void)
{
	int zero, nine, nl;

	zero = 48;
	nine = 57;
	nl = 10;

	for (int = zero; i <= nine; i++)
		putchar(i);

	putchar(nl);
	return (0);
}
